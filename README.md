# Archived Neovim Config

This repository is not the live Neovim source of truth on this machine.

The active config is loaded from:

- `/Users/nishit.gupta/.config/nvim`
- which currently resolves to `/Users/nishit.gupta/dotfiles/nvim/.config/nvim`

If you review or change this repo, do it intentionally as archive or migration work, not as the active editor config.
